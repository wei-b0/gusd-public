"use client";

/**
 * Markets — discovery and comparison for the whole GPU asset class. The
 * front door renders the same surface; execution lives one click deeper.
 */

import { MarketsDiscovery } from "@/components/markets/discovery";

export default function MarketsPage() {
  return <MarketsDiscovery />;
}
